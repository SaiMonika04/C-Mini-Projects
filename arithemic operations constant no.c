#include<stdio.h>
void main()
{
  int a=20,b=10,sum,sub,mul,div;
  sum=a+b;
  sub=a-b;
  mul=a*b;
  div=a/b;
  printf("\n sum of two numbers is %d",sum);
  printf("\n sub of two numbers is %d",sub);
  printf("\n mul of two numbers is %d",mul);
  printf("\n div of two numbers is %d",div);
  getch();
}
